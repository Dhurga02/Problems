// Find Student result based on total marks

const prompt = require("prompt-sync")();

const a = parseInt(prompt("Enter the total marks = "));

if(a >= 35 ){
    console.log(" pass ");

}else{
    console.log(" fail ");
}
