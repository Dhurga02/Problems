// Write a program to find the total chicken price. Input will be how many kilos of chicken, Output will be total chicken price.

const prompt = require("prompt-sync")();

const Chickens = parseInt(prompt("Enter the chicken's = "));

const Kg = parseInt(prompt("Kg is = "));

// const x = Chickens * Kg
 
console.log("otal chicken price = ", Chickens * Kg );
