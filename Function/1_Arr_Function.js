// Create family member names in one array variable and print one by one in the output

const family = ["mom","dad","brother","sister"];
function Names(name){
    for(let i=0 ; i<name.length ; i=i+1){
console.log(name[i]);
    }
}
Names(family);
